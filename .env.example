# Compose-level overrides for the root docker-compose.yml.
# The defaults in docker-compose.yml already match these values.
DB_NAME=vintage_books
DB_USER=postgres
DB_PASSWORD=postgres
DB_PORT=5432
PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=admin
